{
    "name": "warranty-pro-mono",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm run build",
        "install": "npm install --prefix backend && npm install --prefix frontend"
    }
}